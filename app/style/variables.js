import {
  Alignment,
  AnimationStates,
  Boundary,
  Classes as CoreClasses,
  Colors,
  Elevation,
  HandleInteractionKind,
  HandleType,
  Intent,
  Keys,
  PopoverInteractionKind,
  Position,
} from '@blueprintjs/core';
import { IconNames } from '@blueprintjs/icons';
import { Classes as DatetimeClasses } from '@blueprintjs/datetime';
import { Classes as SelectClasses } from '@blueprintjs/select';

const Classes = { ...CoreClasses, ...DatetimeClasses, ...SelectClasses };

export {
  Alignment,
  AnimationStates,
  Boundary,
  Classes,
  Colors,
  Elevation,
  HandleInteractionKind,
  HandleType,
  Intent,
  Keys,
  PopoverInteractionKind,
  Position,
  IconNames,
};
